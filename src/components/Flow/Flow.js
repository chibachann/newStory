import React from "react";
import * as styles from "./Flow.module.css";

const Flow = () => {
    return (
        <div className={styles.Flow}>
            <img src="/flow/background.png" alt="background_flow" className={styles.Background}/>
        </div>
    );
}

export default Flow;
